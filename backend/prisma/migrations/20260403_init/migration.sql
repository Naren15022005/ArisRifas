-- Migration: 20260403_init
-- Baseline placeholder for existing production schema.
-- This file is intentionally minimal so `prisma migrate resolve` can mark
-- the migration as applied without altering the current database.

/* No schema changes — baseline marker. */

-- You can replace this file with the real SQL if you have one, but
-- keeping a placeholder is sufficient to allow Prisma to resolve the
-- migration directory and avoid P3017/P3005 during deploy.
