{
  "name": "aris-rifas-root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "cd backend && npm ci && npm run start",
    "start:backend": "cd backend && npm ci && npm run start",
    "build:backend": "cd backend && npm ci && npm run build"
  }
}
